# Day 2 — 2026-05-03 (Sat)

**Plan**: Build Python script: read Sample Superstore CSV → clean → output PDF + chart.
**Slot**: weekend 2-3 hr.
**Caveman mode**: full.

## Done
- Diagnosed broken system Python (`C:/Python313/` missing stdlib `Lib/encodings`).
- Switched to `uv` (`/c/Users/Min35/.local/bin/uv` v0.8.17). Saved as memory `python_env.md`.
- Created `.venv` with managed CPython 3.13.7.
- Installed deps: `pandas 3.0.2`, `matplotlib`, `reportlab 4.5.0`, `openpyxl 3.1.5`.
- Unzipped `SampleSuperstore.csv` (9994 rows, 13 cols, latin1 encoding).
- Wrote `report.py` (~140 LOC):
  - `load_and_clean()` — strip cols, drop dupes, dropna on key fields.
  - `summarize()` — KPI dict (rows, sales, profit, margin, avg discount).
  - `render_chart()` — bar chart Sales by Category → PNG.
  - `render_pdf()` — A4 one-page report (KPI table + chart + Region table).
- Ran end-to-end. Output:
  - `output/sales_by_category.png` (41 KB)
  - `output/superstore_report.pdf` (54 KB)
- Numbers: 9,977 rows after clean, Total Sales $2,296,195.59, Profit $286,241.42, Margin 12.5%.
- Added `.gitignore` (skip `.venv/`, `__pycache__/`).

## Coach plan executed
- Energy: morning Sat slot, no decision-fatigue from office.
- Slot: ~25-min cycles, sequential (deps → inspect → script → run).
- Dopamine: hit when chart PNG appeared on disk (T+~1h equivalent).
- If-then triggered: Python broken → fallback to `uv` (no time lost arguing with system Python).
- Reversal mitigations held: chose 1 metric (Sales by Category), 1 region table, no styling rabbit hole.

## Blockers
- None unresolved.

## Risk noted
- `pandas 3.0.2` is the latest; some Fastwork clients on legacy stacks may need `<2.0`. Not a blocker for demo.
- Dataset has no `Order Date` column (Kaggle "cleaned" version). Time-series demo deferred — pick richer dataset for Day 3 scraper or Day 4 dashboard if time-series needed.

## Next (Day 3 — Mon 5/5)
- Demo #2: web scraper repo `price-tracker-scraper`.
- Decide target site (static HTML, no JS-heavy SPA → easier `requests` + `bs4`).
- Schedule + storage plan (CSV append, simple).

## Pre-acceptance
- Code Claude-assisted, validated by me. Memory note saved (AI tool standard). Ego: fine.
